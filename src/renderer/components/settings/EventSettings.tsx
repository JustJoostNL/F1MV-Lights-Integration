import React from "react";
import { Button } from "@mui/material";

export function EventSettings() {
  return (
    <Button variant="contained" href="#/event-editor">
      Open Event Editor
    </Button>
  );
}
