import React from "react";
import { Button } from "@mui/material";

export function EffectSettings() {
  return (
    <Button variant="contained" href="#/effect-editor">
      Open Effect Editor
    </Button>
  );
}
