import React from "react";
import { Button } from "@mui/material";

export function DriverAudioSettingsButton() {
  return (
    <Button variant="contained" href="#/driver-audio-editor">
      Open Audio Alert Editor
    </Button>
  );
}
